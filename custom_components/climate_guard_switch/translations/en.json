{
  "config": {
    "step": {
      "user": {
        "title": "Add Climate Guard Switch",
        "description": "What do you want to add?",
        "menu_options": {
          "guard": "Guard switch — protect a heater/cooler relay",
          "history": "History view — one combined history chart (read-only)"
        }
      },
      "guard": {
        "title": "Create Climate Guard Switch",
        "description": "Configure a filtered proxy switch for your climate heater/cooler.",
        "data": {
          "target_entity": "Target Switch Entity (Hardware)",
          "device_type": "Device Type (Heater/Cooler)",
          "sun_entity": "Sun Entity (Optional Check)",
          "weather_entity": "Weather Entity (Optional Check)",
          "climate_entity": "Linked Thermostat (For Manual Override)",
          "allowed_weather_states": "Allowed Weather States (Empty = All)",
          "heartbeat_interval_seconds": "Heartbeat Interval (Seconds)"
        },
        "data_description": {
          "sun_entity": "Optional. When set, the target only runs while the sun is above the horizon.",
          "weather_entity": "Optional. Pairs with Allowed Weather States below to gate the target on current weather.",
          "climate_entity": "Optional. Linking a thermostat lets a manual temperature change bypass an active cooldown once.",
          "allowed_weather_states": "Only used when a Weather Entity is set above. Leave empty to allow any weather.",
          "heartbeat_interval_seconds": "How often the running switch is re-pulsed (keeps the hardware's own auto-off timer refreshed) and how often the Run Limit cutoff is checked. Set to 0 to disable both — including Run Limit enforcement, even if Run Limit itself is set above 0."
        }
      },
      "history": {
        "title": "Create History view",
        "description": "Adds a read-only device that shows a temperature, its target and the heating/cooling periods together in Home Assistant's built-in History page, where you pick your own date range. The entities can come from any integration.\n\nHistory starts when this device is added. The shaded chart keeps as much history as your recorder does (10 days by default, see `purge_keep_days`); the Target temperature, Heating and Cooling sensors are kept as long-term statistics indefinitely.",
        "data": {
          "name": "Name",
          "temperature_sensor": "Temperature sensor",
          "climate_entity": "Thermostat (For The Target Line)",
          "heating_entity": "Heating (Relay Or Switch)",
          "cooling_entity": "Cooling (Relay Or Switch)"
        },
        "data_description": {
          "name": "Used as the device name.",
          "temperature_sensor": "The temperature line of the chart. Heating and cooling are shaded under it, so this is required.",
          "climate_entity": "Optional. Adds the target temperature line and follows the thermostat's mode.",
          "heating_entity": "Optional. Shades the chart while this is on. Pick the physical relay (for example a Shelly switch) so the chart shows what actually ran, not what was requested.",
          "cooling_entity": "Optional. Shades the chart while this is on. Pick the physical relay (for example a Shelly switch) so the chart shows what actually ran, not what was requested."
        }
      },
      "reconfigure": {
        "title": "Reconfigure Climate Guard Switch",
        "description": "Change the device type. Target entity, gates, and heartbeat are configured via Options instead.",
        "data": {
          "device_type": "Device Type (Heater/Cooler)"
        }
      }
    },
    "error": {
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured",
      "history_reconfigure": "History views have no device type — use Configure to change their inputs."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure Climate Guard Switch",
        "data": {
          "target_entity": "Target Switch Entity (Hardware)",
          "device_type": "Device Type (Heater/Cooler)",
          "sun_entity": "Sun Entity (Optional Check)",
          "weather_entity": "Weather Entity (Optional Check)",
          "climate_entity": "Linked Thermostat (For Manual Override)",
          "allowed_weather_states": "Allowed Weather States (Empty = All)",
          "heartbeat_interval_seconds": "Heartbeat Interval (Seconds)"
        },
        "data_description": {
          "sun_entity": "Optional. When set, the target only runs while the sun is above the horizon.",
          "weather_entity": "Optional. Pairs with Allowed Weather States below to gate the target on current weather.",
          "climate_entity": "Optional. Linking a thermostat lets a manual temperature change bypass an active cooldown once.",
          "allowed_weather_states": "Only used when a Weather Entity is set above. Leave empty to allow any weather.",
          "heartbeat_interval_seconds": "How often the running switch is re-pulsed (keeps the hardware's own auto-off timer refreshed) and how often the Run Limit cutoff is checked. Set to 0 to disable both — including Run Limit enforcement, even if Run Limit itself is set above 0."
        }
      },
      "history": {
        "title": "Configure History view",
        "data": {
          "temperature_sensor": "Temperature sensor",
          "climate_entity": "Thermostat (For The Target Line)",
          "heating_entity": "Heating (Relay Or Switch)",
          "cooling_entity": "Cooling (Relay Or Switch)"
        },
        "data_description": {
          "temperature_sensor": "The temperature line of the chart. Heating and cooling are shaded under it, so this is required.",
          "climate_entity": "Optional. Adds the target temperature line and follows the thermostat's mode.",
          "heating_entity": "Optional. Shades the chart while this is on. Pick the physical relay (for example a Shelly switch) so the chart shows what actually ran, not what was requested.",
          "cooling_entity": "Optional. Shades the chart while this is on. Pick the physical relay (for example a Shelly switch) so the chart shows what actually ran, not what was requested."
        }
      }
    }
  },
  "entity": {
    "number": {
      "run_limit": {
        "name": "Run limit"
      },
      "cooldown": {
        "name": "Cooldown"
      }
    },
    "sensor": {
      "status": {
        "name": "Status"
      },
      "target_temperature": {
        "name": "Target temperature"
      },
      "heating": {
        "name": "Heating"
      },
      "cooling": {
        "name": "Cooling"
      }
    },
    "binary_sensor": {
      "active": {
        "name": "Active"
      }
    }
  }
}
