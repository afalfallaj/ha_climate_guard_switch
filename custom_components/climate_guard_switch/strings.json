{
  "config": {
    "step": {
      "user": {
        "title": "Create Climate Guard Switch",
        "description": "Configure a filtered proxy switch for your climate heater/cooler.",
        "data": {
          "target_entity": "Target Switch Entity (Hardware)",
          "device_type": "Device Type (Heater/Cooler)",
          "sun_entity": "Sun Entity (Optional Check)",
          "weather_entity": "Weather Entity (Optional Check)",
          "climate_entity": "Linked Thermostat (For Manual Override)",
          "temperature_sensor": "Temperature Sensor (For History Charts)",
          "allowed_weather_states": "Allowed Weather States (Empty = All)",
          "heartbeat_interval_seconds": "Heartbeat Interval (Seconds)"
        },
        "data_description": {
          "sun_entity": "Optional. When set, the target only runs while the sun is above the horizon.",
          "weather_entity": "Optional. Pairs with Allowed Weather States below to gate the target on current weather.",
          "climate_entity": "Optional. Linking a thermostat lets a manual temperature change bypass an active cooldown once, and adds a Target temperature sensor for history charts.",
          "allowed_weather_states": "Only used when a Weather Entity is set above. Leave empty to allow any weather.",
          "heartbeat_interval_seconds": "How often the running switch is re-pulsed (keeps the hardware's own auto-off timer refreshed) and how often the Run Limit cutoff is checked. Set to 0 to disable both — including Run Limit enforcement, even if Run Limit itself is set above 0.",
          "temperature_sensor": "Optional. Adds a \"Temperature while heating\" (or cooling) sensor that carries this temperature only while the relay is on, so a chart card can mark the heating/cooling periods in the temperature chart for any date range. If empty, the linked thermostat's current temperature is used."
        }
      },
      "reconfigure": {
        "title": "Reconfigure Climate Guard Switch",
        "description": "Change the device type. Target entity, gates, and heartbeat are configured via Options instead.",
        "data": {
          "device_type": "Device Type (Heater/Cooler)"
        }
      }
    },
    "error": {
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured",
      "already_in_progress": "Configuration flow is already in progress",
      "reconfigure_successful": "Re-configuration was successful",
      "history_view_removed": "History views were replaced by the guard's own sensors. Delete this entry and use the sensors on your guard device."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure Climate Guard Switch",
        "data": {
          "target_entity": "Target Switch Entity (Hardware)",
          "device_type": "Device Type (Heater/Cooler)",
          "sun_entity": "Sun Entity (Optional Check)",
          "weather_entity": "Weather Entity (Optional Check)",
          "climate_entity": "Linked Thermostat (For Manual Override)",
          "temperature_sensor": "Temperature Sensor (For History Charts)",
          "allowed_weather_states": "Allowed Weather States (Empty = All)",
          "heartbeat_interval_seconds": "Heartbeat Interval (Seconds)"
        },
        "data_description": {
          "sun_entity": "Optional. When set, the target only runs while the sun is above the horizon.",
          "weather_entity": "Optional. Pairs with Allowed Weather States below to gate the target on current weather.",
          "climate_entity": "Optional. Linking a thermostat lets a manual temperature change bypass an active cooldown once, and adds a Target temperature sensor for history charts.",
          "allowed_weather_states": "Only used when a Weather Entity is set above. Leave empty to allow any weather.",
          "heartbeat_interval_seconds": "How often the running switch is re-pulsed (keeps the hardware's own auto-off timer refreshed) and how often the Run Limit cutoff is checked. Set to 0 to disable both — including Run Limit enforcement, even if Run Limit itself is set above 0.",
          "temperature_sensor": "Optional. Adds a \"Temperature while heating\" (or cooling) sensor that carries this temperature only while the relay is on, so a chart card can mark the heating/cooling periods in the temperature chart for any date range. If empty, the linked thermostat's current temperature is used."
        }
      }
    }
  },
  "exceptions": {
    "history_view_removed": {
      "message": "History views were replaced by the guard's own sensors. Delete this entry and use the sensors on your guard device."
    }
  },
  "entity": {
    "number": {
      "run_limit": {
        "name": "Run limit"
      },
      "cooldown": {
        "name": "Cooldown"
      }
    },
    "sensor": {
      "status": {
        "name": "Status"
      },
      "target_temperature": {
        "name": "Target temperature"
      },
      "temperature_while_heating": {
        "name": "Temperature while heating"
      },
      "temperature_while_cooling": {
        "name": "Temperature while cooling"
      }
    },
    "binary_sensor": {
      "active": {
        "name": "Active"
      }
    }
  }
}
